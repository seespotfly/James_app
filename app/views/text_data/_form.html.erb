<%= form_for(@text_datum) do |f| %>
  <% if @text_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@text_datum.errors.count, "error") %> prohibited this text_datum from being saved:</h2>

      <ul>
      <% @text_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
